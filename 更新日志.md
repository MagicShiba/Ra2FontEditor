## 1.3
布局调整

## 1.2 显式获取系统字体和渲染微调
- 修复静默获取系统字体失败，现在改为显式获取
- 修复字体选择器搜索功能
- 补充一些操作描述
- 添加更多字体粗细选项
- 字体粗细现在可用使用滚轮调整
- 添加转换前预览
- 对齐预览大小
- 原始和转换后预览后面显示宽度
- 适当扩大字符预览显示大小
- 添加渲染位置微调以影响二值化像素位置，但浏览器底层渲染导致y偏移无效，**以后再改**
- 一些界面布局调整

## 1.1 辅助线与渲染偏移
- 添加标尺和辅助线，便于编辑字母字符时对齐
- 辅助线从标尺拖出以添加
- 添加y轴渲染像素偏移，用于解决有的字体上边缘渲染不完整
- 添加分离共用数据开关，可忽略字符分离功能
- 添加批量上移下移按钮
- 一些界面布局调整
- 补全描述
- 添加了一个能够能够显示数字的放大后的字体

## 1.0 基础功能完成
例如：
 - 将系统字体或加载的字体转为ra2格式字体,使用当前字符范围。
 - 缺失的字符直接选中即可添加。
 - 手动编辑任意符号。
 - 可以快速定位字符位置。
 - 批量调整符号宽度和居中，可用于制作等宽字体。
 - 批量位置编辑，以及拼接不同字体。
 - 分离共用数据的占位字符。
 - 预览字体。
 - 简繁转换。
 - 3D阴影字体